- type: serial
  steps:
      - name: pytest
        service: main
        command: bash -c "source ~/.virtualenvs/larissa/bin/activate && pytest --boxed -n 8 --cov=larissa --cov-config=/larissa/.coveragerc /larissa/tests/ && coveralls --base_dir /larissa"
